# Preset Examples

This directory holds example YAML presets for reference and sharing.

- `examples/sensor-focus.yaml` is a sample preset for `tests/fixtures/sensor.csv`.

Runtime loading is user-selected in the app via **Load presets** and does not auto-read this folder.
